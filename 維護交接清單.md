# 法規鑑別清單管理系統 — 維護交接清單

最後更新：2026/06/14（民國115年）

---

## 1. 系統基本資訊

| 項目 | 內容 |
|---|---|
| 系統名稱 | 公信電子股份有限公司 法規鑑別清單管理系統 |
| 線上網址 | https://oscar-hsiao.github.io/law-registry/ |
| 程式碼位置 | https://github.com/oscar-hsiao/law-registry |
| 主要檔案 | `index.html`（單一檔案，含全部前端程式碼與資料） |
| 託管方式 | GitHub Pages（免費、靜態網頁） |
| 法規鑑定人員 | 蕭印廷 (Oscar) |

---

## 2. 三大交接重點（最容易遺漏）

### (1) GitHub 帳號歸屬
- [ ] 確認 `oscar-hsiao/law-registry` repo 的擁有者
- [ ] 若離職後仍需使用本系統，**將 repo Transfer 到公司的 GitHub 組織帳號**
  （Settings → General → Transfer ownership）
- [ ] Transfer 後網址會變為 `https://<公司組織>.github.io/law-registry/`，需更新所有書籤/連結

### (2) Google Apps Script 後端（自動更新功能）
- [ ] 確認程式碼中 `GAS_URL`（在 `index.html` 的 `<script>` 區塊內）是部署在**哪個 Google 帳號**下
- [ ] 若是個人帳號：
  - [ ] 在公司 Google 帳號重新部署一份 GAS 專案
  - [ ] 取得新的 Web App URL
  - [ ] 修改 `index.html` 中的 `GAS_URL = '...'` 為新網址
  - [ ] 測試「自動更新（官方API）」按鈕是否正常運作

### (3) 管理者密碼
- [ ] 交接前**務必更換管理者密碼**，避免離職後舊密碼仍可編輯資料
- [ ] 更換步驟：
  1. 開啟 https://emn178.github.io/online-tools/sha256.html
  2. 輸入新密碼，取得 SHA-256 雜湊值
  3. 修改 `index.html` 中的 `ADMIN_HASH = '...'` 為新的雜湊值
  4. 將新密碼告知接手人員（透過安全管道，不要寫在 commit 訊息或文件中）

---

## 3. 日常維護事項

| 頻率 | 工作內容 | 操作方式 |
|---|---|---|
| 每月 | 點擊「自動更新（官方API）」，檢查法規修正日期 | 需登入管理者模式 |
| 每次更新後 | 檢查標記為「待複核」的法規，確認修正內容並更新合規狀態 | 點擊合規狀態標籤可循環切換 |
| 每季 | 匯出 CSV 備份一份到公司共用資料夾 | 「匯出 CSV」按鈕 |
| 每半年 | 檢視「適用性」欄位是否需要新增/移除法規 | 管理者模式 →「新增法規」/ 編輯 |
| 人員異動時 | 更換管理者密碼（見上方第 2-(3) 項） | 修改 `ADMIN_HASH` |

---

## 4. 修改網站內容的方式

### 簡易修改（不需寫程式）
1. 登入 GitHub → 進入 `law-registry` repo
2. 點 `index.html` → 點鉛筆圖示（Edit）
3. 找到要修改的文字（例如公司名稱、清點日期）
4. 修改後點「Commit changes」
5. 等待約 1 分鐘，網站自動更新

### 本機修改（需安裝 Git）
```bash
git clone https://github.com/oscar-hsiao/law-registry.git
# 修改 index.html
git add index.html
git commit -m "說明修改內容"
git push origin main
```

---

## 5. 常見問題

**Q: 網站打不開 / 顯示 404？**
A: 檢查 GitHub repo 是否還存在、Settings → Pages 是否仍設定為從 `main` branch 部署。

**Q: 點「自動更新」沒有反應或一直失敗？**
A: 多半是 `GAS_URL` 失效（Google 帳號權限變更或專案被刪除），需重新部署 GAS 並更新網址（見第 2-(2) 項）。

**Q: 忘記管理者密碼？**
A: 需要能修改 `index.html` 原始碼的人，重新產生 SHA-256 雜湊值並替換 `ADMIN_HASH`。

**Q: 想換 Logo / 公司名稱？**
A: Logo 圖檔位於 `assets/clientron-logo.png`，直接替換同名檔案即可；公司名稱在 `index.html` 中搜尋「公信電子股份有限公司」逐一替換。

---

## 6. 聯絡資訊（請填寫接手人員資料）

| 角色 | 姓名 | 聯絡方式 |
|---|---|---|
| 原維護者 | 蕭印廷 (Oscar) | |
| 新接手人員 | | |
| GitHub 帳號管理者 | | |
| GAS 後端負責人 | | |
